name: Build App

on:
    push:
        branches:
        - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: build-push
          uses: docker/build-push-action@v1
          with:
            username: ${{ secrets.hipno34 }}
            password: ${{ secrets.Tarasque_34 }}
            registry: docker.io
            repository: hipno34/cd
            tag_with_sha: true
